<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- DatabaseBackup.BACKUP_DB_PATH lives directly in getExternalFilesDir(null). -->
    <external-files-path
        name="backup"
        path="." />
</paths>
